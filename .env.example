# PodAgent OS — Environment Variables

# Anthropic API key for LLM features (context extraction, tangent detection, show notes)
# Optional — pipeline works without it, but with degraded LLM features
ANTHROPIC_API_KEY=sk-ant-...

# HuggingFace token for pyannote model downloads (required for single-track diarization)
# Get yours at: https://huggingface.co/settings/tokens
HF_TOKEN=hf_...
